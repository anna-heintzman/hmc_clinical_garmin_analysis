import pandas as pd

# df["timestamp"] = pd.to_datetime(df["timestamp"])

# df[df["timstamp"] < pd.to_datetime("2019-07-06")]["bpm"].describe()

# for...
# pd.read_csv
# df["timestamp"] = pd.to_datetime(df["timestamp"])

# for day in ["2019-07-08", "2019-07-09"]:
    # df[df["timstamp"] < pd.to_datetime(day)]["bpm"].describe()

    # before descriptive stats. after start of for loop